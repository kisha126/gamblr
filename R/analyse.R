analyze <- function(
    x,
    ...,                     # named expressions like pi_est = 2 / mean(hit)
    summarize = TRUE,        # whether to summarize or return raw expressions
    store_result = TRUE,     # keep results in the simulation object
    verbose = TRUE           # show results immediately
) {

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_init.R
\name{sim_init}
\alias{sim_init}
\title{Initialize a Simulation Context}
\usage{
sim_init(
  constants = list(),
  seed = NULL,
  device = c("cpu", "gpu"),
  name = NULL,
  verbose = FALSE,
  store_history = FALSE,
  backend = "local",
  n_workers = NULL
)
}
\arguments{
\item{constants}{A named list of global constants (e.g., `list(two_pi = 2 * pi)`).}

\item{seed}{An optional integer to set the random seed for reproducibility.}

\item{device}{Target device for simulation, currently `"cpu"` or `"gpu"` (future support).}

\item{name}{Optional simulation name.}

\item{verbose}{Logical; whether to print diagnostic messages during simulation.}

\item{store_history}{Logical; if `TRUE`, stores pipeline steps for reproducibility or debugging.}

\item{backend}{Optional parallel backend, e.g., `"local"`, `"multicore"`, `"slurm"`, or custom.}

\item{n_workers}{Optional integer specifying the number of parallel workers. Ignored if
`backend = "local"` or if parallelization is not supported. Default is `NULL`.}
}
\value{
A simulation context object used for chaining simulation steps.
}
\description{
Starts a new Monte Carlo simulation pipeline with optional constants and a random seed.
}
